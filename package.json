{
  "name": "f10",
  "private": true,
  "workspaces": {
    "packages": [
      "f10-react",
      "f10-stream",
      "f100-test-stream"
    ]
  },
  "devDependencies": {
    "lerna": "^3.5.1"
  },
  "scripts": {
    "build": "lerna exec tsc --stream",
    "test": "lerna run test --stream"
  }
}
